name: PR Build Validation

on:
  pull_request:
    branches:
      - dev
      - main
    paths:
      - "src/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        run: dotnet restore src/Zynapse.Backend.sln

      - name: Build solution
        run: dotnet build src/Zynapse.Backend.sln --no-restore --configuration Release

#      - name: Run tests
#        run: dotnet test src/Zynapse.Backend.Tests/Zynapse.Backend.Tests.csproj --no-build --configuration Release --verbosity normal